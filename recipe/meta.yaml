{% set version = "1.26.7" %}
{% set posix = 'm2-' if win else '' %}

package:
  name: gstreamer_and_plugins
  version: {{ version }}

source:
  - url: https://gstreamer.freedesktop.org/src/gstreamer/gstreamer-{{ version }}.tar.xz
    sha256: 18a5e214114dc501407697dd458514bba62cadd5414c60f793cf70141a4d0bb3
  - url: https://gstreamer.freedesktop.org/src/gst-plugins-base/gst-plugins-base-{{ version }}.tar.xz
    sha256: 969e3cbf05dfab92cf37e94840fbe398517d7ba3275331d1c216a2e30a7208d0
    folder: plugins_base
  - url: https://gstreamer.freedesktop.org/src/gst-plugins-good/gst-plugins-good-{{ version }}.tar.xz
    sha256: 82af18a1f3e4a060db61d2630fbb975269b80b55bb2fdcfddfab5c6440d30781
    folder: plugins_good
    patches:
      - jpeg-win.patch  # [win]
  - url: https://gstreamer.freedesktop.org/src/gst-plugins-bad/gst-plugins-bad-{{ version }}.tar.xz
    sha256: a0ab17dddd4c029ecd7a423c30badd5a3c7599ea42707016d1d57545f5723ccf
    folder: plugins_bad
  - url: https://gstreamer.freedesktop.org/src/gst-plugins-ugly/gst-plugins-ugly-{{ version }}.tar.xz
    sha256: 11f2e4f34bb2e841dc60edc95581572bc47be0c11e577d12bed98bb1f80a661b
    folder: plugins_ugly
  - url: https://gstreamer.freedesktop.org/src/gst-rtsp-server/gst-rtsp-server-{{ version }}.tar.xz
    sha256: 4f234594aea692e3c1bfaa969965039aaf7483bdfc5862b31d614a59e6718abf
    folder: rtsp_server

build:
  number: 1

outputs:
  - name: gstreamer
    script: install_gstreamer.sh  # [unix]
    script: install_gstreamer.bat  # [win]
    build:
      activate_in_script: true
      ignore_run_exports:
        # we need cross-python to build, but this isn't a python package
        - python      # [build_platform != target_platform]
        - python_abi  # [build_platform != target_platform]
      run_exports:
        # remove symbols at .90 patch releases just before minor versions?
        #    https://abi-laboratory.pro/tracker/timeline/gstreamer/
        - {{ pin_subpackage('gstreamer', max_pin='x.x') }}

    requirements:
      build:
        - {{ compiler('c') }}
        - {{ stdlib('c') }}
        - {{ compiler('cxx') }}
        - pkg-config
        - {{ posix }}bison
        - {{ posix }}flex
        - meson
        - ninja
        - gettext-tools
        - gobject-introspection
        # 20221027 - hmaarrfk
        # 3.11 doesn't have distutils which mesons needs???
        - cross-python_{{ target_platform }} 3.10  # [build_platform != target_platform]
      host:
        - python 3.10  # [build_platform != target_platform]
        - glib
        - libintl-devel  # [not linux]
        - libiconv
        - zlib
      run:
        - {{ pin_compatible("glib") }}  # required for anything that builds against GStreamer, e.g. Qt
    test:
      commands:
        - gst-inspect-1.0 --version
        - gst-launch-1.0  --version
        - gst-stats-1.0 --version
        - gst-typefind-1.0 --version
        - test -f $PREFIX/lib/girepository-1.0/Gst-1.0.typelib  # [unix]
        - if not exist %LIBRARY_LIB%\\girepository-1.0\\Gst-1.0.typelib exit 1  # [win]
    about:
      summary: Library for constructing graphs of media-handling components
      description:
      doc_source_url: https://cgit.freedesktop.org/gstreamer/gstreamer/tree/docs
      license: LGPL-2.0-or-later
      license_file: COPYING

  - name: gst-plugins-base
    script: install_base_plugins.sh  # [unix]
    script: install_base_plugins.bat  # [win]
    build:
      activate_in_script: true
      ignore_run_exports:
        # we need cross-python to build, but this isn't a python package
        - python      # [build_platform != target_platform]
        - python_abi  # [build_platform != target_platform]
      run_exports:
        # remove symbols at .90 patch releases just before minor versions?
        #    https://abi-laboratory.pro/tracker/timeline/gstreamer/
        - {{ pin_subpackage('gst-plugins-base', max_pin='x.x') }}

    requirements:
      build:
        - {{ compiler('c') }}
        - {{ stdlib('c') }}
        - {{ compiler('cxx') }}
        - pkg-config
        - meson
        - ninja
        - gettext-tools
        - gobject-introspection
        - cross-python_{{ target_platform }} 3.10    # [build_platform != target_platform]
      host:
        - libgl-devel                        # [linux]
        - libegl-devel                       # [linux]
        - libdrm                             # [linux]
        - python 3.10  # [build_platform != target_platform]
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - glib
        - zlib
        - libintl-devel                      # [not linux]
        - libogg
        - libxcb                             # [linux]
        - libpng                             # [unix]
        - alsa-lib                           # [linux]
        - libopus                            # [unix]
        - libvorbis
        - expat                              # [linux]
        - xorg-libxfixes                     # [linux]
        - xorg-libxext                       # [linux]
        - xorg-xorgproto                     # [linux]
        - xorg-libx11                        # [linux]
        - xorg-libxrender                    # [linux]
        - xorg-libxdamage                    # [linux]
        - xorg-libxxf86vm                    # [linux]
        - xorg-libxau                        # [linux]
        - xorg-libxshmfence                  # [linux]
        # July 17, 2025 -- yishai1999
        # https://github.com/conda-forge/gstreamer-feedstock/pull/155#discussion_r2213275624
        # The GStreamer pango plugin doesn't seem to compile on OSX and windows
        - pango                              # [linux]
      run:
        - {{ pin_subpackage('gstreamer', exact=True) }}

    test:
      commands:
        - test -f $PREFIX/lib/libgstallocators-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstapp-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstaudio-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstfft-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstpbutils-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstriff-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstrtp-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstrtsp-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstsdp-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgsttag-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/libgstvideo-1.0${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/girepository-1.0/GstVideo-1.0.typelib  # [unix]
        - if not exist %LIBRARY_BIN%\\gstallocators-1.0-0.dll exit 1  # [win]
        - if not exist %LIBRARY_LIB%\\girepository-1.0\\GstVideo-1.0.typelib exit 1  # [win]
        - gst-inspect-1.0 --plugin volume
        - gst-inspect-1.0 --plugin pango  # [linux]

    about:
      summary: GStreamer Base Plug-ins
      description: |
        GStreamer Base Plug-ins is a well-groomed and well-maintained collection of
        GStreamer plug-ins and elements, spanning the range of possible types of
        elements one would want to write for GStreamer.
      doc_source_url: https://github.com/GStreamer/gst-plugins-base/tree/master/docs
      license: LGPL-2.0-or-later
      license_file: COPYING

  - name: gst-plugins-good
    script: install_good_plugins.sh  # [unix]
    script: install_good_plugins.bat  # [win]
    build:
      activate_in_script: true
      ignore_run_exports:
        # we need cross-python to build, but this isn't a python package
        - python      # [build_platform != target_platform]
        - python_abi  # [build_platform != target_platform]
      run_exports:
        - {{ pin_subpackage('gst-plugins-good', max_pin='x.x') }}
    requirements:
      build:
        - {{ compiler('c') }}
        - {{ stdlib('c') }}
        - {{ compiler('cxx') }}
        # 20221027 - hmaarrfk - needs distutils, which was removed from 3.11
        - cross-python_{{ target_platform }} 3.10    # [build_platform != target_platform]
        - pkg-config
        - meson
        - ninja
        - gettext-tools
      host:
        - libgl-devel                        # [linux]
        - libegl-devel                       # [linux]
        - libdrm                             # [linux]
        - python 3.10  # [build_platform != target_platform]
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
        - libintl-devel                      # [not linux]
        - openssl
        - libxml2
        - glib
        - libxcb                             # [linux]
        - jack >=1.9.7                       # [linux64]
        - lame                               # [unix]
        - pulseaudio-client                  # [linux]
        - mpg123                             # [linux64 or osx]
        - zlib
        - libsoup                            # [not ppc64le]
        - libjpeg-turbo
        - libpng                             # [unix]
        - bzip2                              # [linux]
        - libxml2                            # [linux]
        - expat                              # [linux]
        - xorg-xorgproto                     # [linux]
        - xorg-libxfixes                     # [linux]
        - xorg-libxext                       # [linux]
        - xorg-libx11                        # [linux]
        - xorg-libxrender                    # [linux]
        - xorg-libxdamage                    # [linux]
        - xorg-libxxf86vm                    # [linux]
        - xorg-libxau                        # [linux]
        - xorg-libxshmfence                  # [linux]
        - libvpx                             # [unix]
      run:
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
        - libsoup                            # [not ppc64le]
        - glib-networking                    # [not ppc64le]
        - xorg-libxfixes                     # [linux]
        - xorg-libxdamage                    # [linux]

    test:
      commands:
        - test -f $PREFIX/lib/gstreamer-1.0/libgstalpha${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/gstreamer-1.0/libgstdebug${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/gstreamer-1.0/libgstjack${SHLIB_EXT}  # [linux64]
        - test -f $PREFIX/lib/gstreamer-1.0/libgstjpeg${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/gstreamer-1.0/libgstlame${SHLIB_EXT}  # [unix]
        - test -f $PREFIX/lib/gstreamer-1.0/libgstmpg123${SHLIB_EXT}  # [linux64 or osx]
        - test -f $PREFIX/lib/gstreamer-1.0/libgstspectrum${SHLIB_EXT}  # [unix]
        - test -f ${PREFIX}/lib/gstreamer-1.0/libgstpulseaudio${SHLIB_EXT}  # [linux]
        - if not exist %LIBRARY_LIB%\\gstreamer-1.0\\gstalpha.dll exit 1  # [win]
        - if not exist %LIBRARY_LIB%\\gstreamer-1.0\\gstdebug.dll exit 1  # [win]
        - if not exist %LIBRARY_LIB%\\gstreamer-1.0\\gstjpeg.dll exit 1  # [win]
        - if not exist %LIBRARY_LIB%\\gstreamer-1.0\\gstspectrum.dll exit 1  # [win]
        - gst-inspect-1.0 --plugin alpha
        - gst-inspect-1.0 --plugin vpx  # [unix]
    about:
      summary: GStreamer Good Plug-ins
      description: |
        GStreamer Good Plug-ins is A collection of plug-ins you'd
        want to have right next to you on the battlefield.
        Shooting sharp and making no mistakes, these plug-ins have it
        all: good looks, good code, and good licensing.  Documented and
        dressed up in tests.  If you're looking for a role model to
        base your own plug-in on here it is.
      doc_source_url: https://github.com/GStreamer/gst-plugins-good/tree/master/docs
      license: LGPL-2.0-or-later
      license_file: COPYING

  - name: gst-plugins-bad
    script: install_bad_plugins.sh  # [unix]
    script: install_bad_plugins.bat  # [win]
    build:
      activate_in_script: true
      ignore_run_exports:
        - python      # [build_platform != target_platform]
        - python_abi  # [build_platform != target_platform]
      run_exports:
        - {{ pin_subpackage('gst-plugins-bad', max_pin='x.x') }}
    requirements:
      build:
        - {{ compiler('c') }}
        - {{ stdlib('c') }}
        - {{ compiler('cxx') }}
        - cross-python_{{ target_platform }} 3.10    # [build_platform != target_platform]
        - pkg-config
        - meson
        - ninja
        - gettext-tools
        - gobject-introspection
      host:
        - python 3.10  # [build_platform != target_platform]
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
        - glib
        - zlib
        - libintl-devel  # [not linux]
        - openssl
        - libsrtp2
        - openh264
      run:
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}

    test:
      commands:
        - gst-inspect-1.0 --plugin dtls
        - gst-inspect-1.0 --plugin openh264

    about:
      summary: GStreamer Bad Plug-ins
      description: |
        GStreamer Bad Plug-ins is a collection of plug-ins that aren't
        up to par compared to the rest. They might be close to being good quality,
        but they're missing something - be it a good code review, some documentation,
        a set of tests, a real live maintainer, or some actual wide use.
      doc_source_url: https://gitlab.freedesktop.org/gstreamer/gst-plugins-bad
      license: LGPL-2.0-or-later
      license_file: COPYING

  - name: gst-rtsp-server
    script: install_rtsp_server.sh  # [unix]
    build:
      skip: true  # [not linux]
      activate_in_script: true
      ignore_run_exports:
        - python      # [build_platform != target_platform]
        - python_abi  # [build_platform != target_platform]
      run_exports:
        - {{ pin_subpackage('gst-rtsp-server', max_pin='x.x') }}
    requirements:
      build:
        - {{ compiler('c') }}
        - {{ stdlib('c') }}
        - {{ compiler('cxx') }}
        - pkg-config
        - meson
        - ninja
        - gettext-tools
        - gobject-introspection
        - cross-python_{{ target_platform }} 3.10    # [build_platform != target_platform]
      host:
        - python 3.10  # [build_platform != target_platform]
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
        - glib
        - zlib
        - openssl
      run:
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
    test:
      commands:
        - test -f $PREFIX/lib/libgstrtspserver-1.0${SHLIB_EXT}  # [linux]
        - test -f $PREFIX/lib/girepository-1.0/GstRtspServer-1.0.typelib  # [linux]
    about:
      summary: GStreamer RTSP Server library
      description: |
        gst-rtsp-server is a library on top of GStreamer for building an RTSP
        server with GStreamer pipelines.
      doc_source_url: https://gitlab.freedesktop.org/gstreamer/gst-rtsp-server
      license: LGPL-2.0-or-later
      license_file: COPYING

  - name: gst-plugins-ugly-lgpl
    script: install_ugly_plugins_lgpl.sh  # [linux]
    build:
      skip: true  # [not linux]
      activate_in_script: true
      ignore_run_exports:
        - python      # [build_platform != target_platform]
        - python_abi  # [build_platform != target_platform]
      run_exports:
        - {{ pin_subpackage('gst-plugins-ugly-lgpl', max_pin='x.x') }}

    requirements:
      build:
        - {{ compiler('c') }}
        - {{ stdlib('c') }}
        - {{ compiler('cxx') }}
        - pkg-config
        - meson
        - ninja
        - gettext-tools
        - gobject-introspection
        - cross-python_{{ target_platform }} 3.10    # [build_platform != target_platform]
      host:
        - python 3.10  # [build_platform != target_platform]
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
        - glib
        - zlib
        - libintl-devel  # [not linux]
      run:
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}

    test:
      commands:
        - gst-inspect-1.0 --plugin asf  # [linux]
        - gst-inspect-1.0 --plugin dvdlpcmdec  # [linux]
        - gst-inspect-1.0 --plugin dvdsub  # [linux]
        - gst-inspect-1.0 --plugin realmedia  # [linux]

    about:
      summary: GStreamer Ugly Plug-ins (LGPL subset)
      description: |
        LGPL-licensed and patent-friendlier modules from the GStreamer Ugly plug-in collection.
      doc_source_url: https://gitlab.freedesktop.org/gstreamer/gst-plugins-ugly
      license: LGPL-2.0-or-later
      license_file: COPYING

  - name: gst-plugins-ugly-gpl
    script: install_ugly_plugins_gpl.sh  # [linux]
    build:
      skip: true  # [not linux]
      activate_in_script: true
      ignore_run_exports:
        - python      # [build_platform != target_platform]
        - python_abi  # [build_platform != target_platform]
      run_exports:
        - {{ pin_subpackage('gst-plugins-ugly-gpl', max_pin='x.x') }}

    requirements:
      build:
        - {{ compiler('c') }}
        - {{ stdlib('c') }}
        - {{ compiler('cxx') }}
        - pkg-config
        - meson
        - ninja
        - gettext-tools
        - gobject-introspection
        - cross-python_{{ target_platform }} 3.10    # [build_platform != target_platform]
      host:
        - python 3.10  # [build_platform != target_platform]
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
        - glib
        - zlib
        - libintl-devel  # [not linux]
        - x264  # [linux]
      run:
        - {{ pin_subpackage('gstreamer', exact=True) }}
        - {{ pin_subpackage('gst-plugins-base', exact=True) }}
        - x264  # [linux]

    test:
      commands:
        - test -f $PREFIX/lib/gstreamer-1.0/libgstx264${SHLIB_EXT}  # [linux]
        - gst-inspect-1.0 --plugin x264  # [linux]

    about:
      summary: GStreamer Ugly Plug-ins (GPL extras)
      description: |
        GPL-licensed additions from the GStreamer Ugly plug-in collection that rely on GPL code such as x264.
      doc_source_url: https://gitlab.freedesktop.org/gstreamer/gst-plugins-ugly
      license: LGPL-2.0-or-later
      license_file: COPYING

about:
  home: https://gstreamer.freedesktop.org/
  summary: Library for constructing graphs of media-handling components
  license: LGPL-2.0-or-later
  license_file: COPYING
  doc_url: https://gstreamer.freedesktop.org/documentation/
  dev_url: https://cgit.freedesktop.org/gstreamer/gstreamer/tree/

extra:
  recipe-maintainers:
    - yishai1999
    - hmaarrfk
    - andfoy
    - ccordoba12
    - mingwandroid
    - msarahan
    - tschoonj
    - scopatz
